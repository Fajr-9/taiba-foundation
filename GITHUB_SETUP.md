# 🔧 إعداد GitHub API للوحة التحكم

## الخطوات المطلوبة

### 1. الحصول على GitHub Personal Access Token

1. اذهب إلى [GitHub Settings > Developer settings](https://github.com/settings/developers)
2. اضغط على "Personal access tokens" ثم "Tokens (classic)"
3. اضغط "Generate new token" ثم "Generate new token (classic)"
4. أدخل اسم للـ Token (مثال: "Tayba Website Admin")
5. اختر صلاحيات (Scopes):
   - ✅ **repo** (Full control of private repositories)
     - هذا ضروري لتحديث الملفات في الـ repository
6. اضغط "Generate token"
7. **⚠️ مهم جداً**: انسخ الـ Token فوراً (لن تتمكن من رؤيته مرة أخرى!)
   - سيبدأ بـ `ghp_` أو `github_pat_`

### 2. معرفة معلومات الـ Repository

تحتاج إلى:
- **اسم المستخدم** (Username): اسمك في GitHub
- **اسم الـ Repository**: اسم الـ repository الذي يحتوي على الموقع

مثال:
- Username: `yourusername`
- Repository: `tayba-website`

### 3. إعداد لوحة التحكم

1. افتح `admin.html` في المتصفح
2. أدخل كلمة المرور: `tayba2024`
3. اذهب لتبويب "الإعدادات"
4. أدخل:
   - **اسم المستخدم**: اسمك في GitHub
   - **اسم الـ Repository**: اسم الـ repository
   - **GitHub Token**: الـ Token الذي حصلت عليه
5. اضغط "حفظ الإعدادات"

### 4. التحقق من الإعداد

1. بعد حفظ الإعدادات، جرب إضافة مشروع جديد
2. إذا ظهرت رسالة "✅ تم حفظ المشاريع بنجاح على GitHub!" فالإعداد صحيح
3. افتح `ongoing.html` أو `completed.html` وتحقق من ظهور المشروع

## 🔒 الأمان

- الـ Token يُحفظ محلياً في المتصفح فقط (localStorage)
- لا يُرسل لأي مكان آخر
- يمكنك حذف الـ Token من GitHub في أي وقت
- كلمة المرور للوحة التحكم: `tayba2024` (يمكن تغييرها في `admin.js`)

## 📝 ملاحظات مهمة

1. **الـ Token يجب أن يكون له صلاحية `repo`**
   - بدون هذه الصلاحية لن يتمكن من تحديث الملفات

2. **اسم الـ Repository**
   - إذا كان الموقع على `username.github.io`، اسم الـ Repository هو `username.github.io`
   - إذا كان على domain مخصص، اسم الـ Repository هو اسم الـ repo الذي أنشأته

3. **التحديثات فورية**
   - بعد الحفظ، قد يستغرق GitHub Pages دقيقة أو دقيقتين لتحديث الموقع
   - يمكنك إضافة `?t=timestamp` في نهاية URL لإجبار المتصفح على تحديث الـ cache

## 🆘 حل المشاكل

### المشكلة: "فشل في الحفظ" أو "Bad credentials"
- تأكد من أن الـ Token صحيح
- تأكد من أن الـ Token له صلاحية `repo`
- تأكد من أن اسم المستخدم واسم الـ Repository صحيحين

### المشكلة: "Not found"
- تأكد من أن اسم الـ Repository صحيح
- تأكد من أن الملف `projects.json` موجود في الـ repository

### المشكلة: "Resource not accessible by integration"
- الـ Token قد لا يكون له الصلاحيات المطلوبة
- أنشئ token جديد مع صلاحية `repo` كاملة

## 🔄 تحديث الـ Token

إذا أردت تغيير الـ Token:
1. اذهب لتبويب "الإعدادات" في لوحة التحكم
2. أدخل الـ Token الجديد
3. اضغط "حفظ الإعدادات"

## 📞 الدعم

إذا واجهت أي مشاكل:
1. افتح Console في المتصفح (F12) وافحص الأخطاء
2. تأكد من أن جميع المعلومات صحيحة
3. تأكد من أن الـ Token له الصلاحيات المطلوبة
